apiVersion: networking.k8s.io/v1
kind: IngressRoute
metadata:
  name: api-ingress
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`sae503-dev.kaze-cloud.fr`)
      kind: Rule
      services:
        - name: api-quotes
          port: 5000
      middlewares:
        - name: ratelimit
