apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-search-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.rule: "Host(`sae503-prod.kaze-cloud.fr`) && PathPrefix(`/search`)"
spec:
  rules:
  - host: sae503-prod.kaze-cloud.fr
    http:
      paths:
      - path: /search
        pathType: Prefix
        backend:
          service:
            name: api-search
            port:
              number: 5000